apiVersion: v1
kind: Service
metadata:
  name: {{ include "democalculator.fullname" . }}
  labels:
    {{- include "democalculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "democalculator.selectorLabels" . | nindent 4 }}
