apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "democalculator.fullname" . }}-test-connection"
  labels:
    {{- include "democalculator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "democalculator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
